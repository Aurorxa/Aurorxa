# [tools] - 开发工具版本
[tools]
node = "24"
pnpm = "10"

# [env] - 环境变量
[env]

# [tasks] - 项目任务
[tasks.install]
description = "安装依赖"
run = "pnpm install"

[tasks.build]
description = "构建"
run = "pnpm docs:build"

[tasks.dev]
description = "开发"
run = "pnpm docs:dev"

[tasks.preview]
description = "预览"
depends = ["build"]
run = "pnpm docs:preview"
